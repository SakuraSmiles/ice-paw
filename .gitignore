# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Tauri
target/

# Machine-specific config（含本地绝对路径；** 任意深度——src-tauri 下的机器级 cargo 配置同样忽略）
**/.cargo/config.toml
.claude/settings.json
start-dev.cmd

# Demo seed scripts（含本地路径，仅供个人开发使用）
seed_demo.mjs
seed_demo.sql

# Native binaries (libsodium)
sodium-prebuilt/
sodium-dist/

# Bundled runtimes (prepared by scripts/prepare-*.ps1, large binaries, NOT checked in)
packages/app/src-tauri/resources/mcp-runtime/node/

# Test artifacts
test-results/
*.tsbuildinfo

# Preview screenshots
_preview-*.png
_preview-*.html

# Backup files
*.bak
.icepaw-backup/

# Environment
.env
.env.*
# 例外：允许 .env.example 提交（仅作模板，不含真实密钥）
!.env.example

# Database
*.db
*.sqlite
backup-data.sql
restore-data.py

# 品牌设计草稿区（选色卡/色彩架构决策卡等 HTML，本地工作产物不入库）
brand/

# docx 语料（真实工程文档，用户拍板严禁版本控制/上传；仅本地供 corpus 测试运行时读取）
packages/app/src-tauri/tests/fixtures/docx/*.docx
