# §3.2.3 项目管理能力 + §3.2.4 模板管理能力
> v4 深度拆分版 | 拆分规则：或/或者→拆、以及/同时/并/且→拆、多种输入方式→各自独立、多步骤→每步独立、与连接独立行为→拆
> 涉及：前端 2.3/2.4/2.5/2.11/2.12/2.13/2.14 → 后端 3.5/3.7/3.8/3.9/3.11/3.12

---

## 3.2.3 项目管理能力
> 项目 CRUD + 项目-会话关联。前端 `ProjectManagerPage`（2.3）、`ProjectCard/ProjectFormModal`（2.11.2/2.11.3）、`ProjectSelector.vue`（2.12.6）、`projects.ts`（2.14.4）、`conversations.ts`（2.14.3）；后端 `project_cmd.rs`（3.8）、`conversation_cmd.rs`（3.5）、`repo/project.rs`（3.11.5）。

### 3.2.3.1 项目创建

**REQ-PROJ-001 项目名称填写**
- a. 标识符：REQ-PROJ-001
- b. 需求描述：用户通过 `ProjectFormModal.vue`（2.11.3）填写项目创建表单的名称字段，名称为必填，≤50 字符。
- c. 异常行为：名称为空 → `AppError::InvalidInput`，表单名称框下标红。
- d. 合格性方法：TEST

**REQ-PROJ-001a 项目描述填写**
- a. 标识符：REQ-PROJ-001a
- b. 需求描述：用户通过 `ProjectFormModal.vue`（2.11.3）填写描述字段，描述为选填，≤200 字符。
- c. 异常行为：描述超过 200 字符 → 前端截断禁止输入，不提交后端。
- d. 合格性方法：TEST

**REQ-PROJ-001b 项目主题色选择**
- a. 标识符：REQ-PROJ-001b
- b. 需求描述：用户通过 `ProjectFormModal.vue`（2.11.3）中主题色选择器，从 `projectAccent.ts`（2.18.2）预设色板中选取颜色。
- c. 异常行为：未选择主题色 → 使用默认色板首项，不报错。
- d. 合格性方法：TEST

**REQ-PROJ-001c 项目创建提交**
- a. 标识符：REQ-PROJ-001c
- b. 需求描述：表单提交后，`projects.ts`（2.14.4）调用后端 `project_cmd.rs`（3.8）`create_project` 创建项目。
- c. 异常行为：① 名称重复 → `AppError::InvalidInput`，表单名称框下标红；② DB 写入失败 → store 回滚乐观更新，Toast"项目操作失败，请重试"。
- d. 合格性方法：TEST

**REQ-PROJ-001d 项目创建后展示**
- a. 标识符：REQ-PROJ-001d
- b. 需求描述：`create_project` 成功后返回 `project_id`，`ProjectCard.vue`（2.11.2）展示新项目卡片。
- c. 异常行为：卡片渲染失败 → Toast"项目已创建但显示异常，请刷新"。
- d. 合格性方法：TEST

### 3.2.3.2 项目编辑

> 原始 REQ-PROJ-002/002a 各包含「失焦**或** Enter 触发」+「防抖提交」三个独立行为，拆分为 6 条。

**REQ-PROJ-002 项目名称失焦触发保存**
- a. 标识符：REQ-PROJ-002
- b. 需求描述：`ProjectCard.vue`（2.11.2）中项目名称行内编辑器失去焦点时触发保存操作。
- c. 异常行为：名称为空 → `AppError::InvalidInput`，行内编辑框下标红，恢复编辑前值。
- d. 合格性方法：TEST

**REQ-PROJ-002a 项目名称 Enter 键触发保存**
- a. 标识符：REQ-PROJ-002a
- b. 需求描述：`ProjectCard.vue`（2.11.2）中项目名称行内编辑器按 Enter 键时触发保存操作。
- c. 异常行为：名称为空 → `AppError::InvalidInput`，行内编辑框下标红，恢复编辑前值。
- d. 合格性方法：TEST

**REQ-PROJ-002b 项目名称保存防抖提交**
- a. 标识符：REQ-PROJ-002b
- b. 需求描述：项目名称变更触发保存后，经 300ms 防抖合并连续变更，调用后端 `update_project` 提交。
- c. 异常行为：① 名称重复 → `AppError::InvalidInput`，行内编辑框下标红；② DB 写入失败 → 回滚至编辑前值，Toast"保存失败，请重试"。
- d. 合格性方法：TEST（防抖合并 + DB 故障回滚）

**REQ-PROJ-002c 项目描述失焦触发保存**
- a. 标识符：REQ-PROJ-002c
- b. 需求描述：`ProjectCard.vue`（2.11.2）中项目描述行内编辑器失去焦点时触发保存操作。
- c. 异常行为：无独立异常（描述字段选填，空值合法）。
- d. 合格性方法：TEST

**REQ-PROJ-002d 项目描述 Enter 键触发保存**
- a. 标识符：REQ-PROJ-002d
- b. 需求描述：`ProjectCard.vue`（2.11.2）中项目描述行内编辑器按 Enter 键时触发保存操作。
- c. 异常行为：无独立异常。
- d. 合格性方法：TEST

**REQ-PROJ-002e 项目描述保存防抖提交**
- a. 标识符：REQ-PROJ-002e
- b. 需求描述：项目描述变更触发保存后，经 300ms 防抖合并连续变更，调用后端 `update_project` 提交。
- c. 异常行为：DB 写入失败 → 回滚至编辑前值，Toast"保存失败，请重试"。
- d. 合格性方法：TEST（防抖合并 + DB 故障回滚）

### 3.2.3.3 项目删除

**REQ-PROJ-003 项目删除二次确认**
- a. 标识符：REQ-PROJ-003
- b. 需求描述：`ProjectCard.vue`（2.11.2）提供删除按钮，点击弹出 Popconfirm（1.13）二次确认。
- c. 异常行为：删除被取消 → 中止操作无副作用。
- d. 合格性方法：TEST

**REQ-PROJ-003a 项目删除执行**
- a. 标识符：REQ-PROJ-003a
- b. 需求描述：二次确认通过后调用后端 `delete_project`，后端执行项目记录删除。
- c. 异常行为：① 项目不存在 → `AppError::NotFound`，Toast"项目不存在或已被删除"；② DB 删除失败 → Toast"项目删除失败，请重试"，卡片保持可见。
- d. 合格性方法：TEST

**REQ-PROJ-003b 项目删除级联解绑**
- a. 标识符：REQ-PROJ-003b
- b. 需求描述：项目删除后，`repo/project.rs`（3.11.5）级联解除关联会话的 `project_id`（设为 NULL，不删会话数据）。
- c. 异常行为：级联更新失败 → 项目删除操作整体回滚（事务）。
- d. 合格性方法：TEST（级联解除关联一致性）

### 3.2.3.4 项目-会话关联

> 原始 REQ-PROJ-004 包含「分页查询」+「分组展示」两个独立行为，拆分为 2 条。

**REQ-PROJ-004 项目会话分页查询**
- a. 标识符：REQ-PROJ-004
- b. 需求描述：`conversations.ts`（2.14.3）调用后端 `conversation_cmd.rs`（3.5），按 `project_id` 分页查询该项目下的会话列表。
- c. 异常行为：分页查询无结果 → 返回空列表，不报错。
- d. 合格性方法：TEST

**REQ-PROJ-004a 项目会话分组展示**
- a. 标识符：REQ-PROJ-004a
- b. 需求描述：`ProjectSelector.vue`（2.12.6）按项目维度分组展示会话列表，每个项目分组下展示该项目关联的会话条目。
- c. 异常行为：分组内无会话 → 展示 `EmptyChatHint`（2.8.5）空状态提示。
- d. 合格性方法：TEST

**REQ-PROJ-004b 项目按 ID 查询**
- a. 标识符：REQ-PROJ-004b
- b. 需求描述：后端提供 `get_project_by_id` 查询接口，按 `project_id` 返回项目详情（名称、描述、主题色等）。
- c. 异常行为：`project_id` 不存在 → 返回 `AppError::NotFound`。
- d. 合格性方法：TEST

**REQ-PROJ-004c 已删除项目会话回退分组**
- a. 标识符：REQ-PROJ-004c
- b. 需求描述：项目删除后，原本绑定的会话 `project_id` 已被设为 NULL，这些会话在前端分组中显示在"未分类"分组下。
- c. 异常行为：无异常路径。
- d. 合格性方法：TEST（级联绑定一致性）

### 3.2.3.5 路由守卫

**REQ-PROJ-005 路由 UUID 格式校验**
- a. 标识符：REQ-PROJ-005
- b. 需求描述：路由 `/chat/:projectId` 经 `router/index.ts`（2.6）`beforeEnter` 守卫校验 `projectId` 参数格式是否为合法 UUID。
- c. 异常行为：`projectId` 非 UUID 格式 → 守卫直接重定向 `/project-manager`，不发起后端查询。
- d. 合格性方法：TEST（UUID 格式校验）

**REQ-PROJ-005a 路由项目不存在重定向**
- a. 标识符：REQ-PROJ-005a
- b. 需求描述：路由 `/chat/:projectId` 的 `beforeEnter` 守卫通过 `get_project_by_id` 校验 `projectId` 对应项目是否存在。项目不存在时重定向到 `/project-manager`。
- c. 异常行为：项目已被删除后会话页面刷新 → 守卫检测到项目不存在，重定向 `/project-manager`，Toast"项目不存在"。
- d. 合格性方法：TEST（重定向边界）

**REQ-PROJ-005b 路由项目存在无会话处理**
- a. 标识符：REQ-PROJ-005b
- b. 需求描述：路由 `/chat/:projectId` 的 `beforeEnter` 守卫校验通过（项目存在），但该项目下无关联会话时，展示 `EmptyChatHint.vue`（2.8.5）空状态提示。
- c. 异常行为：无异常路径。
- d. 合格性方法：TEST

**REQ-PROJ-005c 路由项目存在有会话正常进入**
- a. 标识符：REQ-PROJ-005c
- b. 需求描述：路由 `/chat/:projectId` 的 `beforeEnter` 守卫校验全部通过（项目存在且有会话），正常进入聊天页面。
- c. 异常行为：无异常路径。
- d. 合格性方法：TEST

---

## 3.2.4 模板管理能力
> 提示模板 CRUD + 上下文管线渲染注入。前端 `TemplateManagerPage`（2.4）、`TemplateForm/TemplateList`（2.13.2/2.13.3）、`TemplateCards.vue`（2.8.11）、`templates.ts`（2.14.6）；后端 `template_cmd.rs`（3.9）、`repo/template.rs`（3.11.7）、`context/template.rs`（3.12.8）。

### 3.2.4.1 模板创建

**REQ-TMPL-001 模板名称输入**
- a. 标识符：REQ-TMPL-001
- b. 需求描述：用户通过 `TemplateForm.vue`（2.13.2）填写模板名称字段，名称为必填，≤50 字符。
- c. 异常行为：名称为空 → `AppError::InvalidInput`，名称字段下标红。
- d. 合格性方法：TEST

**REQ-TMPL-001a 模板名称重复校验**
- a. 标识符：REQ-TMPL-001a
- b. 需求描述：提交模板创建时，后端校验模板名称在全局范围内的唯一性。
- c. 异常行为：名称重复 → `AppError::DuplicateName`，名称字段旁提示"名称已使用"。
- d. 合格性方法：TEST

**REQ-TMPL-001b 模板内容输入**
- a. 标识符：REQ-TMPL-001b
- b. 需求描述：用户通过 `TemplateForm.vue`（2.13.2）填写模板内容字段，内容为必填，支持 `{{variable}}` 占位符语法，≤5000 字符。
- c. 异常行为：内容为空 → `AppError::InvalidInput`，内容字段下标红。
- d. 合格性方法：TEST

**REQ-TMPL-001c 模板描述输入**
- a. 标识符：REQ-TMPL-001c
- b. 需求描述：用户通过 `TemplateForm.vue`（2.13.2）填写模板描述字段，描述为选填。
- c. 异常行为：无异常路径。
- d. 合格性方法：TEST

**REQ-TMPL-001d 模板创建提交**
- a. 标识符：REQ-TMPL-001d
- b. 需求描述：表单提交后，`templates.ts`（2.14.6）调用后端 `template_cmd.rs`（3.9）`create_template` 创建模板，返回 `template_id`。
- c. 异常行为：DB 写入失败 → Toast"模板创建失败，请重试"。
- d. 合格性方法：TEST

**REQ-TMPL-001e 模板创建后列表展示**
- a. 标识符：REQ-TMPL-001e
- b. 需求描述：模板创建成功后，`TemplateList.vue`（2.13.3）卡片列表中展示新模板卡片。
- c. 异常行为：卡片渲染失败 → Toast"模板已创建但显示异常，请刷新"。
- d. 合格性方法：TEST

### 3.2.4.2 模板搜索

**REQ-TMPL-002 模板名称搜索筛选**
- a. 标识符：REQ-TMPL-002
- b. 需求描述：`TemplateList.vue`（2.13.3）卡片列表上方提供搜索框，用户输入关键词后按名称对模板列表进行实时筛选。
- c. 异常行为：搜索无匹配结果 → 展示空状态提示"未找到匹配的模板"。
- d. 合格性方法：TEST（搜索筛选）

### 3.2.4.3 模板编辑

> 原始 REQ-TMPL-002a 包含「点击进入编辑」+「提交保存」两个独立步骤，拆分为 2 条。

**REQ-TMPL-002a 模板编辑入口**
- a. 标识符：REQ-TMPL-002a
- b. 需求描述：用户点击 `TemplateList.vue`（2.13.3）中模板卡片，进入 `TemplateForm.vue`（2.13.2）编辑模式，表单回填当前模板的名称、内容、描述。
- c. 异常行为：模板不存在 → `AppError::NotFound`，Toast"模板不存在或已被删除"。
- d. 合格性方法：TEST

**REQ-TMPL-002b 模板编辑提交保存**
- a. 标识符：REQ-TMPL-002b
- b. 需求描述：用户在 `TemplateForm.vue` 编辑完成后点击提交，调用后端 `update_template` 保存修改。
- c. 异常行为：① 名称重复 → `AppError::DuplicateName`，名称字段旁提示"名称已使用"；② 模板不存在 → `AppError::NotFound`。
- d. 合格性方法：TEST

### 3.2.4.4 模板删除

**REQ-TMPL-002c 模板删除确认**
- a. 标识符：REQ-TMPL-002c
- b. 需求描述：`TemplateList.vue`（2.13.3）中模板卡片提供删除操作，点击后弹出确认对话框，需用户确认后方可执行删除。
- c. 异常行为：删除被取消 → 中止操作无副作用。
- d. 合格性方法：TEST

**REQ-TMPL-002d 模板删除引用检查**
- a. 标识符：REQ-TMPL-002d
- b. 需求描述：删除确认通过后调用后端 `delete_template`，`repo/template.rs`（3.11.7）检查模板是否被 Agent 引用。
- c. 异常行为：① 模板被 Agent 引用时删除 → `AppError::ResourceInUse`，列出引用该模板的 Agent 名称列表，Toast"无法删除：[列表]"；② 模板不存在 → `AppError::NotFound`。
- d. 合格性方法：TEST（引用检查）

### 3.2.4.5 变量定义校验

**REQ-TMPL-003 创建时变量名校验**
- a. 标识符：REQ-TMPL-003
- b. 需求描述：`repo/template.rs`（3.11.7）在模板创建时校验占位符变量名仅含 `[a-zA-Z0-9_]` 字符。
- c. 异常行为：① 非法占位符（`{{}}` 空变量名或含特殊字符如空格、中文、符号）→ `AppError::InvalidInput`，附错误位置（行号/变量名），内容区下标红；② 多个非法占位符 → 返回首个错误，不批量报告。
- d. 合格性方法：INSP（变量解析正则）+ TEST

**REQ-TMPL-003a 编辑时变量名校验**
- a. 标识符：REQ-TMPL-003a
- b. 需求描述：`repo/template.rs`（3.11.7）在模板编辑时校验占位符变量名仅含 `[a-zA-Z0-9_]` 字符。
- c. 异常行为：① 非法占位符 → `AppError::InvalidInput`，附错误位置（行号/变量名），内容区下标红；② 多个非法占位符 → 返回首个错误，不批量报告。
- d. 合格性方法：INSP（变量解析正则）+ TEST

### 3.2.4.6 模板注入对话上下文

> 原始 REQ-TMPL-004 包含「选择模板」+「附带发送」两个独立行为，拆分为 2 条。
> 原始 REQ-TMPL-004a 包含「查询模板」+「替换变量」两个独立步骤，拆分为 2 条。

**REQ-TMPL-004 模板选择**
- a. 标识符：REQ-TMPL-004
- b. 需求描述：用户通过 `TemplateCards.vue`（2.8.11）选择一个模板，所选 `template_id` 记录到前端发送状态中。
- c. 异常行为：无异常路径。
- d. 合格性方法：TEST

**REQ-TMPL-004a 模板 ID 随消息发送**
- a. 标识符：REQ-TMPL-004a
- b. 需求描述：用户发送消息时，已选模板的 `template_id` 附带在 `SendMessageRequest` 中提交到后端。
- c. 异常行为：未选择模板时 `template_id` 为 null，不影响消息正常发送。
- d. 合格性方法：TEST

**REQ-TMPL-004b 模板内容查询**
- a. 标识符：REQ-TMPL-004b
- b. 需求描述：`context/template.rs`（3.12.8）阶段根据 `SendMessageRequest` 中的 `template_id` 从数据库查询模板内容。
- c. 异常行为：`template_id` 不存在 → 跳过模板渲染阶段，日志 ERROR"模板不存在"，不阻断后续管线。
- d. 合格性方法：TEST

**REQ-TMPL-004c 模板变量渲染替换**
- a. 标识符：REQ-TMPL-004c
- b. 需求描述：`context/template.rs`（3.12.8）将模板内容中的 `{{variable}}` 占位符替换为 `template_variables` 键值对中对应的具体值。
- c. 异常行为：① 变量未绑定（`template_variables` 中无对应 key）→ 保留原始 `{{variable}}` 文本（不静默改空），日志 WARN"模板变量未绑定"；② 变量值长度 >500 字符 → 截断至 500 字符后追加"…"，日志 WARN。
- d. 合格性方法：TEST（变量完整 / 缺失 / 超长单元测试）

**REQ-TMPL-004d 模板渲染结果注入管线**
- a. 标识符：REQ-TMPL-004d
- b. 需求描述：模板渲染后的最终内容注入上下文管线，位置在 `system_prompt.rs`（3.12.6）之后、`history.rs`（3.12.4）之前，以 `[模板: <模板名称>]` 标记包裹。
- c. 异常行为：渲染内容为空（模板未找到或未选择） → 跳过注入步骤，不阻断管线。
- d. 合格性方法：TEST + DEMO

---

## 拆分变更摘要

### 3.2.3 项目管理能力

| 原始 ID | 拆分原因 | 拆分后 |
|---------|---------|--------|
| REQ-PROJ-002 | 「失焦**或** Enter」→ 规则1；「触发保存 + 防抖提交」→ 规则4 | REQ-PROJ-002（失焦触发）+ 002a（Enter触发）+ 002b（防抖提交） |
| REQ-PROJ-002a | 同上 | REQ-PROJ-002c（失焦触发）+ 002d（Enter触发）+ 002e（防抖提交） |
| REQ-PROJ-004 | 「分组展示 + 分页查询」→ 规则4 | REQ-PROJ-004（分页查询）+ 004a（分组展示） |
| REQ-PROJ-004a | 重编号 | REQ-PROJ-004b |
| REQ-PROJ-004b | 重编号 | REQ-PROJ-004c |

项目管理：17 条 → 22 条

### 3.2.4 模板管理能力

| 原始 ID | 拆分原因 | 拆分后 |
|---------|---------|--------|
| REQ-TMPL-001 | 异常描述修正（空名→InvalidInput，非 DuplicateName） | REQ-TMPL-001（修正异常） |
| REQ-TMPL-002a | 「点击进入编辑 + 提交保存」→ 规则4 | REQ-TMPL-002a（编辑入口）+ 002b（提交保存） |
| REQ-TMPL-002b | 重编号 | REQ-TMPL-002c |
| REQ-TMPL-002c | 重编号 | REQ-TMPL-002d |
| REQ-TMPL-004 | 「选择模板 + **一并**发送」→ 规则2（并） | REQ-TMPL-004（模板选择）+ 004a（ID 随消息发送） |
| REQ-TMPL-004a | 「查询模板 + 替换变量」→ 规则4 | REQ-TMPL-004b（内容查询）+ 004c（变量渲染替换） |
| REQ-TMPL-004b | 重编号 | REQ-TMPL-004d |

模板管理：15 条 → 18 条

**总计：原始 32 条 → 拆分后 40 条**
