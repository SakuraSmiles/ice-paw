// @ice-paw/ui 全量注册入口
// 阶段 1 骨架 — 空导出，后续按需补充
import type { App } from 'vue'

const IcePawUI = {
  install(_app: App) {
    // 阶段 2+：app.component('IpButton', Button) 逐个注册
  },
}

export default IcePawUI
