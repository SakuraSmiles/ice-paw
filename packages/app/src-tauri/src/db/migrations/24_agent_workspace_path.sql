-- =========================================================================
-- 24: Agent 工作区路径（Phase 3）
-- 说明：指向存放 agent.yaml 的本地目录，行为层配置从此文件读取
-- =========================================================================

ALTER TABLE agents ADD COLUMN workspace_path TEXT DEFAULT NULL;
