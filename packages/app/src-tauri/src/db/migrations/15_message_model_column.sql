-- =========================================================================
-- IcePaw 数据库迁移 V15：消息级模型记录
-- 来源：聊天页架构决策 4b — 每条 AI 消息展示实际使用的模型名
-- 说明：
--   - 新增 messages.model 列（TEXT，可为 NULL）
--   - NULL = 历史消息（迁移前已存在）
--   - 非 NULL = 该条消息实际使用的模型名（如 'gpt-4o' / 'claude-sonnet-4'）
--   - ALTER TABLE ADD COLUMN 默认追加到末尾，旧数据自动 NULL
-- =========================================================================

ALTER TABLE messages ADD COLUMN model TEXT;