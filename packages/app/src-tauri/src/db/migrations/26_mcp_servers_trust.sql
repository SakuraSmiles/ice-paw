-- =========================================================================
-- IcePaw 数据库迁移 V26：MCP Server 信任级别
-- Phase 2: 按 Server 维度控制工具授权级别
-- =========================================================================

-- trust_level 列已在 25_mcp_servers.sql 的 CREATE TABLE 中定义。
-- 此迁移仅对使用旧版 25.sql 的数据库生效。
-- 对于列已存在的数据库，ALTER TABLE 会报 duplicate column 错误，
-- 但由于该列已存在且数据一致，此错误可安全忽略。
-- sqlx 迁移遇到错误时会终止，所以改为空操作迁移。
SELECT 1;
