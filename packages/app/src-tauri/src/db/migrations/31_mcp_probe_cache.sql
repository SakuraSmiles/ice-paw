-- =========================================================================
-- IcePaw 数据库迁移 V31：MCP Server probe 结果持久化
-- 缓存最后一次成功 probe 的工具清单，加速工具集页面首屏渲染，
-- 并在网络临时不通时提供降级展示（避免直接显示"未就绪"）。
-- =========================================================================

ALTER TABLE mcp_servers ADD COLUMN probe_cache TEXT DEFAULT NULL;
